# PARKED

This experiment was scoped before desk's identity was locked as
"M5-first, platform-second, LEE as a future tenant" (2026-04-27, see
`.context/runs/2026-04-27-experiment-pass-1.md`).

It assumed pi-the-CLI would be a primary desk client. Once the M5
became the forcing-function client, exp-05c (the M5 frame renderer)
absorbed the client-rendering work and exp-05a / exp-05b were
deferred indefinitely.

Pi-as-a-desk-client may still be useful (it's the only headless
client format), but it would need a fresh question framed against
today's protocol, not the v0 spec this README was written against.

If you're an agent picking up "make a CLI/TUI client": skip this
file and write a fresh experiment.
