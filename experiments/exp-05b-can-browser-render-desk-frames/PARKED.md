# PARKED

Same parking reason as exp-05a: this was scoped before the M5
became desk's forcing-function client. exp-05c (M5 renderer)
absorbed the work; a browser client was never needed for v0.

A browser client is plausibly useful as a remote-control / desktop
companion surface, but it would need a fresh question framed against
today's protocol (frame ops, manifest schema, MCP-driven inbox).

If you're an agent picking up "make a browser client": skip this
file and write a fresh experiment.
